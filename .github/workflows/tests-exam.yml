name: tests-exam

on:
  pull_request:
    paths:
      - 'exam/**'
  
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: cd exam && npm install && npm run test:ci
